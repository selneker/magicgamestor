# URL publique du backend Render (sans slash final). Le code construit REACT_APP_BACKEND_URL + /api
# Doit être définie AVANT le build (variable de build Static Site Render).
REACT_APP_BACKEND_URL=https://magicgamestore-api-v2.onrender.com
